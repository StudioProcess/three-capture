{
  "name": "three-capture",
  "version": "0.0.1",
  "description": "",
  "repository": "github:StudioProcess/three-capture",
  "engines": {
    "node": ">=8.9.0",
    "npm": ">=5.6.0"
  },
  "scripts": {
    "start": "browser-sync start --server --port 8080 --no-notify --files index.html app",
    "test": "echo \"Error: no test specified\" && exit 1",
    "reinstall": "rm -rf node_modules && npm install"
  },
  "keywords": [],
  "author": "Process <contact@process.studio> (https://process.studio)",
  "license": "AGPL-3.0",
  "dependencies": {
    "ccapture.js": "^1.0.7",
    "three": "^0.90.0"
  },
  "devDependencies": {
    "browser-sync": "^2.23.6"
  }
}
